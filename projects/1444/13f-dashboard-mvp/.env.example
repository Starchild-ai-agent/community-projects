# No environment variables required.
# The dashboard reads src/summary.json directly.
# To rebuild summary.json from SEC, run src/build_summary.py
# (requires public internet access to www.sec.gov, no API key).
